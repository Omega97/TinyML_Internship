# 2026-07-03

## Dati e modelli disponibili (snapshot)

### Dati — raw

| Asset | Path | Note |
|-------|------|------|
| **Lc0 training chunks** | `data/raw/lc0/chunks/` | **54 866** `.gz` (2 shard, ~1.15 GiB); manifest `data/raw/lc0/manifest.json` |
| **Lc0 tars** | `data/raw/lc0/tars/` | 2 archivi `.tar` verificati (SHA256 in manifest) |
| **Kaggle Lichess** | `data/raw/games.csv` | ~7.3 MB — smoke / statistiche bucket; **non** per NNUE |
| **Piece-count analysis** | `excel/piece_count_distribution_10k.xlsx` | Riferimento distribuzione bucket (Kaggle) |

### Dati — processed

| Asset | Path | Note |
|-------|------|------|
| **Posizioni Lc0 (pilot)** | `data/processed/lc0/positions.parquet` | FEN + `bucket_id` + metadati Lc0; **senza label Stockfish** |
| **Split train/val** | `data/processed/lc0/splits/` | train **1 063** · val **60** (pilot `--total 2000`, 120 chunk) |
| **Stats gate** | `data/processed/lc0/stats/` | `bucket_survival_sweep.csv`, `filter_pass_report.json` |

Filtri pilot: `min_ply=32` (16 mosse), bucket 7 relax `min_ply=8`. Vedi [NOTES/Lc0 preprocessing pipeline.md](NOTES/Lc0%20preprocessing%20pipeline.md).

### Modelli — SARDINE (attivo)

| Componente          | Stato                         | Path                                       |
| ------------------- | ----------------------------- | ------------------------------------------ |
| **Feature encoder** | ✅ 716 dim, dual POV, 8 bucket | `src/tinymlinternship/features/`           |
| **Engine eval**     | ✅ HCE (placeholder)           | `src/tinymlinternship/engine/eval_hce.py`  |
| **Search**          | ✅ alpha-beta + qsearch (v0.3) | `src/tinymlinternship/engine/search.py`    |
| **Perft**           | ✅ d5 startpos                 | `src/tinymlinternship/engine/perft.py`     |
| **NNUE bucketed**   | ❌ non addestrata              | target `716→16→32→1` × 8 — `models/` vuoto |
| **int8 export Wio** | ❌                             | post-training                              |

### Modelli — legacy (pre-SARDINE)

| Asset | Path | Note |
|-------|------|------|
| Checkpoints value/policy | `legacy/pre-sardine/models/checkpoints/` | 10× `.pt` (nano/tiny/…/policy_lab) — **non** compatibili con encoder 716 |
| Sketch Wio | `legacy/pre-sardine/Arduino/Wio_TinyValueTest/` | int8 headers da pipeline 768-dim |

### Test suite

**66 test** passanti (features 29 · engine 16 · perft 7 · viz · download_lc0 · lc0_parser · lc0_preprocess).

---

## The Plan for Today

Contesto: step 1 encoder ✅ · engine **v0.3** (HCE + alpha-beta + qsearch) ✅ · GIF in `images/sardine_game.gif` (ancora 1-ply) ✅ · Lc0 raw + pilot processed ✅.  
Obiettivo: **step 2** search skeleton su PC — perft, alpha-beta e quiescence base chiusi; carry-over: TT, `--depth` su GIF, label SF.

1. **Pulizia doc rimasta da ieri**
   - [ ] `index_map.py` — riferimento `NOTES/SARDINE 🐟.md` → [NOTES/SARDINE Engine Blueprint.md](NOTES/SARDINE%20Engine%20Blueprint.md)
   - [ ] `encoder.py` — docstring castling: diritti da `base` + swap K/Q se mirror
   - [ ] Aggiornare [ai-feed.md](ai-feed.md) (step 1 chiuso, engine v0.1, architettura 32→1 expert)

2. **Search skeleton — perft**
   - [x] Modulo perft in `src/tinymlinternship/engine/perft.py` (movegen via `python-chess` v0.1)
   - [x] Test: `perft(5)` startpos = **4 865 609** (`tests/test_perft.py`)

3. **Alpha-beta a profondità fissa**
   - [x] `search(board, depth)` — negamax + alpha-beta (`engine/search.py` v0.2)
   - [x] `eval` pluggable (`EvalFn`, default HCE)
   - [x] Test: mating move d2, cattura vincente d2, startpos d2 (`tests/test_engine.py`)

4. **Quiescence (minima)**
   - [x] Estensione catture/promozioni in foglia (`search.py` v0.3, `quiescence=True` default)
   - [x] Test promozione: senza qsearch regina su h1 persa a Kxh1 (`test_quiescence_avoids_hanging_promotion`)

5. **Integrazione CLI & GIF**
   - [x] `run_engine.py` — flag `--depth` (default 1)
   - [ ] `record_engine_game.py` — flag `--depth`
   - [ ] Rigenerare `images/sardine_game.gif` con search più profonda; confronto qualità

6. **Dati Lc0** *(fatto oggi)*
   - [x] `scripts/download_lc0.py` — subset ~1.15 GiB
   - [x] Parser V6 → FEN, pipeline preprocess, stats gate, pilot parquet
   - [ ] Label Stockfish su split train (dopo stats su più chunk)
   - [ ] Comandi in [NOTES/SARDINE commands.md](NOTES/SARDINE%20commands.md)

---

## Stato a fine giornata

| Fatto oggi | Carry-over |
|------------|------------|
| Perft d5 + alpha-beta + quiescence (engine **v0.3.0**) | TT prototype + benchmark |
| Lc0 download + preprocess pilot | Label Stockfish, train NNUE |
| Encoder 716 + 29 test golden | Parity device (step F) |
| **66 test** passanti | `record_engine_game.py --depth`, GIF depth ≥2 |
| Doc encoder / ai-feed | Pulizia `index_map.py`, `encoder.py` |

Focus Phase 2 (blueprint): TT → depth 3–4 tuning → integrazione CLI/GIF.  
Blueprint: [NOTES/SARDINE Engine Blueprint.md](NOTES/SARDINE%20Engine%20Blueprint.md) · Mission: [Goal.md](Goal.md) · TODO: [TODOs.md](TODOs.md)

---

## Execution Log

- **Lc0 download** — subset ~1.15 GiB, 54 866 chunk; `scripts/download_lc0.py`.
- **Lc0 parser smoke** — V6 classical → FEN; `lc0_parser.py`, `smoke_test_lc0_chunk.py`.
- **Pipeline preprocessing** — nota [NOTES/Lc0 preprocessing pipeline.md](NOTES/Lc0%20preprocessing%20pipeline.md); feedback `ai-feed.md` integrato (mosse vs ply, Fase 4/5, bucket 7).
- **Stats gate** — `stats_lc0_processed.py`: bucket 7 → 458 posizioni post-filtro su 80 chunk (OK per N/8).
- **Pilot dataset** — `prepare_lc0_dataset.py` → `positions.parquet` + splits (1 063 train / 60 val).
- **Perft** — `engine/perft.py`; depth 1–5 startpos + kiwipete d1; `tests/test_perft.py` (7 test).
- **Alpha-beta** — `search(board, depth, eval_fn=…)`; `run_engine.py --depth`; engine **v0.2.0**; +6 test in `test_engine.py`.
- **Quiescence** — capture-only qsearch at depth-0 leaves; `quiescence=False` opt-out; engine **v0.3.0**; +3 test in `test_engine.py`.