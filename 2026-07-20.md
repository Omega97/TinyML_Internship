# 2026-07-20

## Context (snapshot)

| Area | Stato |
| ---- | ----- |
| **Encoder** | ✅ **844** dim (716 base + tactical 128), dual POV, 8 buckets |
| **Engine** | ✅ v0.3 — perft d5, alpha-beta, capture quiescence, MVV-LVA |
| **NNUE pilot** | ✅ `pilot_W128_844` val_mse **0.056** — wired via `--eval nnue` |
| **Bot eval (d1, 16g)** | NNUE **~1465** Elo · HCE **~400** · Sunfish **~400** (ACPL heuristic) |
| **Bot eval (d2 no-q, 16g)** | HCE **~2610** · NNUE **400** · Sunfish **400** — NNUE collapses at d2 |
| **Teacher / labels** | ✅ Uniform labeling (C) — Lichess **2371** + Lc0 **3149** con teacher `791556` |
| **Production data** | ✅ C · D · **E smoke** (mini set end-to-end) |
| **Device (Wio)** | ❌ Not started |
| **Test suite** | ✅ **106 passed** (2026-07-20) |

**Focus oggi:** chiudere il **mini production set** — merge D → smoke train E → hygiene.  
Blueprint: [NOTES/SARDINE Engine Blueprint.md](NOTES/SARDINE%20Engine%20Blueprint.md) · [TODOs.md](TODOs.md) · ieri: [2026-07-17.md](2026-07-17.md)

---

## The Plan for Today

**Focus:** production pipeline D→E a scala smoke/pilot (coerenza end-to-end).

1. **Merge + manifest (checklist D)** ✅
   - [x] `merge_training_sets.py` → `train.parquet` / `val.parquet` + `manifest.json`
   - [x] Verifica: split by `game_id`, `source` ∈ {lichess, lc0}, un solo teacher, bucket counts

2. **Loader / smoke train (checklist E)** ✅
   - [x] Una short train smoke su `train.parquet` (pochi epoch) — conferma colonne + label end-to-end
   - [x] Annotare se il loader accetta lo schema production senza patch

3. **Hygiene + chiusura** ✅
   - [x] Sync `TODOs.md` (pipeline C: label mini + merge + smoke train)
   - [x] `py -3.12 -m pytest tests/ -q` → **106 passed** in 324.8 s
   - [x] Execution log + carry-over

**Explicitly not today:**
- Full Lichess dump / overnight labeling (F)
- nnue-pytorch fork, prune, PTQ
- Qsearch polish / TT / nodes-s
- Wio / C port / accumulators
- Idea 07-18 (bit pezzi iniziali su board) — solo se tempo extra

---

## Execution Log

### 1. Merge + manifest (D) — teacher `791556.pb.gz`

- **Command:** `merge_training_sets.py -i lichess_labeled.parquet lc0_labeled.parquet -o data/processed/labeled --val-fraction 0.05 --seed 42`
- **Outputs:**
  - `data/processed/labeled/train.parquet` — **5306** rows, **152** games
  - `data/processed/labeled/val.parquet` — **214** rows, **8** games
  - `data/processed/labeled/manifest.json`
- **Checks:**
  - split by `game_id`: **0** overlap train/val ✓
  - `source` ∈ {lichess, lc0} — train lc0=3004 / lichess=2302; val lc0=145 / lichess=69 ✓
  - un solo teacher `791556.pb.gz` (sha256 `b1c7047582a8…`) ✓
  - `expected_reward` ∈ **[-1, +1]** (mean ≈ +0.011) ✓
  - bucket counts (all): `{0:240, 1:129, 2:1186, 3:159, 4:1707, 5:22, 6:883, 7:1194}` — bucket **5** rare (22); val manca 0/1/3/5 (sample piccolo)
- **Note:** val è thin (8 games / 5% by game) — ok per smoke; non usare per metriche per-bucket definitive

### 2. Loader / smoke train (E)

- **Loader gap:** `ChessbenchDataset` richiedeva `white_features`/`black_features` precomputate — **non** accettava lo schema production (solo `fen` + `expected_reward`) senza patch.
- **Patch:** `src/tinymlinternship/nnue/dataset.py` — se mancano le feature, encode on-the-fly con `encode_dual(fen)`; test `test_dataset_encodes_from_fen` ✅
- **Smoke command:**
  ```
  py -3.12 scripts/train_nnue.py --train data/processed/labeled/train.parquet --val data/processed/labeled/val.parquet --epochs 2 --batch-size 256 --hidden-dim 128 --run-name smoke_prod_W128_844
  ```
- **Result:** 5306 train / 214 val · 110 216 params · ~8 s/epoch (CPU, encode-on-fly)
  - epoch 1: train_mse **0.343** · val_mse **0.269**
  - epoch 2: train_mse **0.236** · val_mse **0.247** (best)
- **Checkpoint:** `models/checkpoints/nnue/smoke_prod_W128_844/{best.pt,last.pt,config.json,history.json}`
- **Note:** val_mse ~0.25 >> pilot ChessBench 0.056 — atteso (2 epoch, mini set teacher-labeled, val thin); goal era coerenza pipeline, non Elo

### 3. Hygiene + chiusura

- **pytest:** `py -3.12 -m pytest tests/ -q` → **106 passed** in **324.8 s** (era 100 il 2026-07-16; +encode-from-fen e suite preesistente)
- **TODOs.md:** pipeline C aggiornata — label mini ✅, merge mini ✅, smoke production path ✅; aperti full-volume labeling e nnue-pytorch
- **Giorno chiuso sul focus D→E:** mini production set end-to-end ok

---

## Carry-over (next session)

1. **F** Full Lichess dump + large-scale FEN extract + re-label same teacher (volume)
2. nnue-pytorch fork/adapt (844-dim bucketed, prune)
3. Longer train on mini (or merged) set if useful before scale-up
4. Qsearch depth limit / delta pruning (non-blocking)
5. TT / nodes-s benchmark

## Parking lot

- **2026-07-18 idea:** eval input bit su pezzi iniziali ancora sulla scacchiera (+1 extra per pezzi promossi, es. 2 regine / 3 torri)
