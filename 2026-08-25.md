# 2026-08-25

## Context

Goal §2 WDL student vs README: single DualHidden net, 3-way STM softmax, soft CE. Carry-over from [DAILY-NOTES/2026-08/2026-08-24.md](DAILY-NOTES/2026-08/2026-08-24.md).

**Note**: The disparity in training/test positions makes it so that the test set might be a bit easier than the training set. For example, the training set also contains difficult puzzles.

## Plan for today

1. Align `train_nnue.py` / `DualHiddenNNUE` / `features.npz` with README WDL spec.
2. Point the test holdout at a slice that exists on disk.

## Execution log

- **Trainer = README.** Single `DualHiddenNNUE` (no buckets): L1 64×2, L2 128, head 3 logits + `Softmax`. Loss: soft CE on STM `wdl`. Default test slice `…_100000-105000_d80_draw5` (old `100000-101000` is gone). Dataset load rejects npz without `wdl` `(N,3)`.
- **npz scan:** most dump slices are v2 WDL and valid. Missing npz: `60000-65000_d90`, `65000-70000_d90`. kaggle/mini/puzzles have WDL arrays but stale meta (will re-encode on train load).
- **Dataset count (mattina, 21 parquet):** **568 648** EPD unici, **592 970** righe, visits **636 554**, overlap **24 322**.
- **Linear WDL trainer.** `LinearWDLNNUE` + `scripts/train_linear_wdl.py`: no hidden layers, concat `[STM ‖ opp]` 1688 → 3 logits + softmax, soft CE. Default val = `…_0-5000_d90`.
- **Linear smoke 100 ep, lr=1e-3:** train pool ~905k / 9 step/epoch. Best `test_ce=0.870` (ancora in calo). Plot: `plots/linear_wdl_smoke_ce.png`. Log in `ai-feed.md`.
- **Linear full 50 ep, lr=0.01** (stesso `--run-name linear_wdl_smoke`): pool 967 258 in 33 slice, 472 step/epoch. Best **epoch 9 `test_ce=0.764509`**, MAE 0.251. Plateau dopo ~epoch 5–10; test CE sotto train (no overfit). Ckpt `models/checkpoints/nnue/linear_wdl_smoke/best.pt`.
- **Inspect linear best.pt:** 1 000 posizioni casuali del test (`seed=0`, slice `…_0-5000_d90`). Top 20 best ≈ scacchi secchi (CE≈0); top 20 worst CE 1.68–4.29 (segno WDL invertito vs Lc0). Output in `ai-feed.md`. `inspect_nnue_positions.py`: load `linear_wdl` / `medium_wdl`, `--sample`/`--seed`.
- **Medium WDL.** `MediumWDLNNUE` (844×2 → **20 CReLU** → 3 softmax, 33 843 params) + `scripts/train_medium_wdl.py`. Stesso split del lineare. Smoke / full commands nel carry-over.
- **Puzzle WDL ricalcolati (Lc0 depth 2).** Slice ora `fen_value_visits_lichess_puzzles_depth2/` (4 000 righe, visits 4 000). JSON+parquet hanno STM `wdl` (mean ≈ `[0.552, 0.256, 0.192]`, 18.5% con max≥0.95). **`features.npz` è stale:** max |Δ| vs parquet ≈ 0.999; il train userà i WDL vecchi finché non si fa `--rebuild`. Meta dice `depth=2` ma `source` punta ancora al path `…/fen_value_visits_lichess_puzzles/`.
- **Dataset count (sera, `scripts/count_fen_value_visits.py` su parquet):** **42 slice**, **1 176 263** righe totali, **1 116 173** EPD unici (fields 1–4), visits sum **1 270 558**, overlap cross-slice **60 090**. Dump Lichess `2026-07` copre 0–200000 (più `d80_draw5` / `d99` su 100000–105000) + puzzles_depth2. Nota: il count legge il **parquet**; il val `…_0-5000_d90` ha parquet **11 350** righe vs npz **31 378** (train linear ha usato l’npz).
- **Dataset in costruzione (misura attuale, parquet).** `py -3.12 -u scripts/count_fen_value_visits.py --quiet`: **62 slice**, **1 850 016** righe, **1 742 445** EPD unici, visits **1 992 734**, overlap **107 571**. Crescita vs sera: +20 slice, +~674k righe, +~626k EPD. Dump Lichess `2026-07` continua a espandersi; Goal §1 ancora aperto.
- **Training in corso (Goal §2).** Linear WDL (`scripts/train_linear_wdl.py`, 5 067 params) sul dump live, test `…_0-5000_d90`. Run recente: pool ~1.60M in 54 slice, 781 step/epoch, `lr=0.01`, epoch 00 CE≈`log(3)`. Plot: `plots/linear_wdl_ce.png` (anche in `README.md` § Training). Trainer: `--test` / `--test-slice`, `--test-subset-size`, `--train-val-subset-size` (frozen `train_val_ce` confrontabile con `test_ce`). Medium (`H=20`) e DualHidden L1 64×2 + L2 128 ancora da lanciare sul pool aggiornato.
- **Holdout leak.** `train_nnue.py` test `…_100000-105000_d80_draw5` lasciava in train il sibling `…_100000-105000_d99` (stesse partite). `test_ce` crollava a ~0.17 vs train ~0.73. Fix: `overlapping_dump_slices` esclude ogni slice dump con range `[start,end)` sovrapposto. Stesso holdout in linear/medium.
- **Trainer UX.** Plot sempre `{run_name}_ce.png`; log/plot solo `train_ce` e `test_ce` (niente `train_val_ce` / MAE). `--lr-end` opzionale (default `None` = LR costante); se impostato, decay lineare `--lr` → `--lr-end`.
- **Barriera CE 0.70.** NNUE 128-neuron DualHidden `dual_h128_H256_fast` (L1 128×2, L2 256, ~test `…_0-5000_d90`, `--fast`): **train_ce=0.690** (epoch 199), **test_ce=0.710** (epoch 200). Prima volta sotto / al bordo di **0.70** su train e test (prima i linear/medium restavano ~0.76–0.77). Ckpt `models/checkpoints/nnue/dual_h128_H256_fast/`.

## Carry-over

1. Rebuild npz puzzle depth-2: `py -3.12 -u scripts/encode_slice_features.py data/processed/board_eval/fen_value_visits/fen_value_visits_lichess_puzzles_depth2 --rebuild`
2. DualHidden non-`--fast` (full epoch) L1 128 / L2 256, `--lr-end 0.001`, run-name nuovo.
3. Allineare parquet vs npz sul val `…_0-5000_d90` prima di fidarsi del count come pool di train.

## Commands run

```powershell
py -3.12 -m pytest tests/test_nnue_model.py tests/test_fen_value_visits.py -q
py -3.12 -u scripts/count_fen_value_visits.py
py -3.12 -u scripts/train_linear_wdl.py --epochs 100 --smoke --run-name linear_wdl_smoke --plot plots/linear_wdl_smoke_ce.png
py -3.12 -u scripts/train_linear_wdl.py --epochs 50 --lr 1e-2 --run-name linear_wdl_smoke --plot plots/linear_wdl_smoke_ce.png
py -3.12 -u scripts/inspect_nnue_positions.py linear_wdl_smoke --ckpt best --n 20 --sample 1000 --seed 0 --slice fen_value_visits_lichess_db_standard_rated_2026-07_0-5000_d90
py -3.12 -u scripts/count_fen_value_visits.py --quiet
py -3.12 -u scripts/train_linear_wdl.py --epochs 20 --lr 0.01 --run-name linear_wdl --plot plots/linear_wdl_ce.png
```
