# 2026-07-16

## Context (snapshot)

| Area | Stato |
| ---- | ----- |
| **Encoder** | ✅ **844** dim (716 base + tactical 128), dual POV, 8 buckets |
| **Engine** | ✅ v0.3 — perft d5, alpha-beta, capture quiescence, MVV-LVA |
| **NNUE pilot** | ✅ `pilot_W128_844` val_mse **0.056** — wired via `--eval nnue` |
| **Bot eval (d1, 16g)** | NNUE **~1465** Elo · HCE **~400** · Sunfish **~400** (ACPL heuristic) |
| **Bot eval (d2 no-q, 16g)** | HCE **~2610** · NNUE **400** · Sunfish **400** — NNUE collapses at d2 |
| **Teacher / labels** | ✅ Lc0 + `label_positions.py` (smoke + 50 Lichess FENs) |
| **Production data** | 🔄 Scripts ready; ideal train/val set **not** built (see below) |
| **Device (Wio)** | ❌ Not started (C port, accumulators, encoder parity) |
| **Test suite** | ✅ **100 passed** (2026-07-16) |

**Bottleneck:** production training path (C → train). Ideal set defined in [ASSETS.md](ASSETS.md); smoke only today. Blueprint: [NOTES/SARDINE Engine Blueprint.md](NOTES/SARDINE%20Engine%20Blueprint.md) · [TODOs.md](TODOs.md)

---

## The Plan for Today

**Focus:** unlock production data (Lichess → FEN → batch labels). Keep search polish and device work off the critical path.

1. **`lichess_pgn_to_fen.py`** — streaming PGN → FEN + `bucket_id` (and piece_count / has_queen as needed) ✅
   - [x] Scaffold script under `scripts/`
   - [x] Smoke on a small PGN (or first N games) → parquet/CSV with `fen`, `bucket_id`
   - [x] Quick check: all 8 buckets appear (or document skew)

2. **Batch labeling via `label_positions.py`** ✅
   - [x] Run on smoke FEN set from step 1 (or existing ChessBench sample beyond 10)
   - [x] Confirm labels ∈ [-1, +1], log throughput (positions/s)
   - [x] Note any UCI/timeout issues for larger batches

3. **Hygiene (if time)** ✅
   - [x] README demo links → `images/hce_d2_game.gif` / `images/nnue_d2_game.gif` (or `images/games/depth2_game_demo.gif`)
   - [x] `py -3.12 -m pytest tests/ -q` after any script/lib changes
   - [x] Sync `TODOs.md` checkboxes for pipeline C

4. **Chiusura**
   - [x] Execution log + carry-over for next session

**Explicitly not today (unless blocked elsewhere):**
- Qsearch depth limit / delta pruning (HCE)
- TT / nodes-s benchmark
- nnue-pytorch fork, prune, PTQ
- Wio / C port / incremental accumulators

---

## Execution Log

### 1. `lichess_pgn_to_fen.py` — scaffold + smoke

- **Script:** `scripts/lichess_pgn_to_fen.py` — streaming `chess.pgn.read_game`, columns `fen`, `bucket_id`, `piece_count`, `has_queen`, `ply`, `game_index`, `game_id`, `result`, `white`, `black`, `site`. Flags: `--max-games`, `--max-positions`, `--min-ply`, `--sample-every`, `--no-startpos`. Writes parquet/CSV + `*.stats.json`.
- **Smoke input:** `data/raw/lichess_smoke50.pgn` — 50 games (≥16 moves) built from Kaggle `data/raw/games.csv` (SAN → PGN). Engine self-play PGNs alone were skewed (missing queenless midgame buckets).
- **Output:** `data/processed/lichess/smoke_fens.parquet` — **2 371** positions, 50 games, 0 broken.
- **Bucket counts (all 8 present):**

| bucket | 0 | 1 | 2 | 3 | 4 | 5 | 6 | 7 |
|--------|---|---|---|---|---|---|---|---|
| n | 117 | 129 | 303 | 159 | 514 | 22 | 626 | 501 |

- **Skew note:** bucket **5** (p=28–31, no queens) is rare in short human games (22/2371). Engine d1 PGNs also miss 2–3 buckets depending on play style; real Lichess dumps should improve natural mix.
- Stats: `data/processed/lichess/smoke_fens.stats.json`

### 2. Batch labeling — `label_positions.py` on Lichess smoke FENs

- **Command:** `label_positions.py --input data/processed/lichess/smoke_fens.parquet --output data/processed/labeled/lichess_smoke_labeled.parquet --limit 50 --verbose`
- **Network:** `791556.pb.gz` (fast), backend blas
- **Result:** **50/50** labeled, all `expected_reward` ∈ **[-1, +1]** (min −0.999, max +1.0, mean **+0.380**)
- **Throughput:** wall **8.48 s** → **~5.9 positions/s** (includes process start; no UCI timeouts/errors)
- **Output:** `data/processed/labeled/lichess_smoke_labeled.parquet` (cols: `fen`, `expected_reward`, `wdl_win/draw/loss`)
- **Scale note:** at ~6 pos/s, 10k positions ≈ **28 min**, 100k ≈ **4.7 h** (CPU OpenBLAS). For production batches, prefer overnight jobs or stronger hardware; BT4 would be much slower — keep `fast` for labeling volume.

### 3. Hygiene

- **README** — demo table: `depth2_game_demo.gif`, `images/hce_d2_game.gif`, `images/nnue_d2_game.gif` + reproduce commands; link to `ASSETS.md`; scripts list includes `lichess_pgn_to_fen.py` / `label_positions.py`
- **pytest** — `py -3.12 -m pytest tests/ -q` → **100 passed** in 71.8 s (was 94 on 2026-07-10)
- **TODOs.md** — pipeline C: PGN→FEN script + label batch smoke checked; full Lichess dump / large batch still open

### 4. Chiusura

- Session plan items 1–3 complete. Production path still needs real Lichess volume + full labeling + nnue-pytorch train.

### A. Schema & tooling (ASSETS ideal set)

- **`schema.py`** — `PRELABEL_COLUMNS` / `LABELED_REQUIRED_COLUMNS`, `ensure_prelabel_columns`, `split_by_game`, `build_manifest` / `write_manifest`, `sha256_file`
- **`lichess_pgn_to_fen.py`** — rows include `stm_white`, `source=lichess`
- **`lc0_preprocess.py`** — `ParsedPosition` + export use ASSETS names (`bucket_id`, not only internal `bucket`)
- **`label_positions.py`** — loads full frame, preserves columns, adds `expected_reward` / WDL / `teacher_network`
- **`merge_training_sets.py`** — multi-input merge → `train.parquet`, `val.parquet`, `manifest.json` (game-level split, single teacher check)
- **Tests** — `test_dataset_schema.py` + lc0 preprocess helper updates; merge smoke OK

### B. Unlabeled position extract

| Source | Path | Rows | Games | Buckets |
| ------ | ---- | ---- | ----- | ------- |
| **Lichess** (smoke50 PGN) | `data/processed/lichess/positions.parquet` | **2 371** | 50 | **all 8** (bucket 5 rare: 22) |
| **Lc0** (120 chunks, natural) | `data/processed/lc0/positions.parquet` | **3 149** | 107 | missing **1, 3, 5** (queenless mid bands absent in this pilot filter set) |

- Schema: both have full pre-label columns (`fen`, `bucket_id`, `piece_count`, `has_queen`, `stm_white`, `ply`, `source`, `game_id`).
- Lichess stats: `data/processed/lichess/positions.stats.json`
- Lc0: `positions.parquet` = all filter-pass (natural); `splits/` = stratified pilot only; `manifest.json` updated.
- Report: `data/processed/unlabeled_extract_report.json`
- **Note:** Lc0 missing buckets are data/filter skew, not schema bugs. Lichess smoke supplies 1/3/5 for merge diversity until a larger Lc0 scan or real Lichess dump.

---

## Production dataset — TODOs (vs [ASSETS.md](ASSETS.md) ideal set)

Goal: **complete, uniform, coherent** training data — Lichess primary + Lc0 supplement may stay in **separate files**, but every production row shares the same eval:

> **`expected_reward`** = White-POV expected outcome ∈ [-1, +1] from **one** fixed Lc0 net (not `best_q`, not game result, not ChessBench/SF values).

See ASSETS §Uniformity rule. Optional merge → `train`/`val` + `manifest.json` after both blocks are labeled the same way.

### Uniformity checklist (all blocks)

- [ ] **Same target column** on every labeled production parquet: `expected_reward` (required for train)
- [ ] **Same formula** everywhere: Lc0 WDL → White POV (document in manifest `label_formula`)
- [ ] **Same `teacher_network`** on Lichess block and Lc0 block (reject mixed nets)
- [ ] **Do not train on** Lc0 `best_q` / `root_q` even if present as debug columns
- [ ] **Do not mix** ChessBench / Kaggle outcome rows into production train without re-labeling under this policy
- [ ] Loader / train code uses **only** `expected_reward` as the regression target
- [ ] Manifest records teacher + formula once for the whole run (covers all blocks)

### Can we do the whole thing today?

| Scope | Today? | Why |
| ----- | ------ | --- |
| **Full ideal set (10⁵–10⁶+ rows, real Lichess monthly PGN)** | **No** | Monthly dumps are multi‑GB download; labeling at ~**6 pos/s** ⇒ 100k ≈ **4.7 h**, 1M ≈ **2 days** continuous CPU; not a same-day finish. |
| **Schema-uniform mini production set (coherent end-to-end)** | **Yes** | Use existing smoke PGN + Lc0 pilot FENs; extend scripts; label a few k positions; write `train`/`val` + manifest. Proves the pipeline; size is not blueprint-scale. |
| **Mid volume (e.g. 10k–50k labels) overnight** | **Partial** | Feasible if a job is left running; still not “full Lichess”. |

**Recommendation:** treat **full volume** as multi-session; use the checklist below. Today (or next short session): items **A–E** (uniform mini set). Leave **F–G** for when a real Lichess dump is available.

---

### A · Schema & tooling (uniform rows) ✅

- [x] Align **pre-label** outputs to ASSETS columns: `fen`, `bucket_id`, `piece_count`, `has_queen`, `stm_white`, `ply`, `source`, `game_id`
  - [x] `lichess_pgn_to_fen.py` — `stm_white`, `source="lichess"` (headers kept as optional)
  - [x] `lc0_preprocess.positions_to_dataframe` — `bucket_id`, `source="lc0"`, `has_queen`, `stm_white`
- [x] Align **labeler** — preserve metadata + `teacher_network` + reward range check
- [x] **`merge_training_sets.py`** — merge labeled parquets; split by `game_id`; `train`/`val`/`manifest.json`
- [x] **`src/tinymlinternship/data/schema.py`** — column contract, `split_by_game`, `build_manifest`, sha256 helper
- [x] Tests: `tests/test_dataset_schema.py` + updated `test_lc0_preprocess.py`

### B · Position extract (unlabeled) ✅ (mini / pilot scale)

- [x] Lichess: `data/processed/lichess/positions.parquet` (not only `smoke_fens.parquet`)
  - [x] Short path: regenerate from `data/raw/lichess_smoke50.pgn` with full schema
  - [ ] Full path (later): download monthly PGN → `data/raw/lichess/` → stream extract
- [x] Lc0: promote pilot to production-shaped unlabeled table
  - [x] Re-run `prepare_lc0_dataset.py` → full ASSETS schema; **natural** all filter-pass → `positions.parquet`
  - [x] Stratified train/val kept only under `splits/` (pilot exploration)
- [x] Sanity: bucket histograms + `data/processed/unlabeled_extract_report.json`

### C · Labeling (single teacher, uniform `expected_reward`)

Both sources stay separate files if useful; **both** must get the same eval column from the same teacher.

- [ ] Freeze teacher for this dataset: e.g. **`791556` (fast)** — same net for Lichess **and** Lc0 (do not mix nets)
- [ ] Label Lichess FENs → `data/processed/labeled/lichess_labeled.parquet` with column **`expected_reward`** (full extract, not `--limit 50` only)
- [ ] Label Lc0 FENs → `data/processed/labeled/lc0_labeled.parquet` with the **same** `expected_reward` formula (ignore / do not copy chunk `best_q` into the target)
- [ ] Cross-block check: both files have `expected_reward` + identical `teacher_network` value; all labels ∈ **[-1, +1]**
- [ ] Log positions/s and wall time; throughput budget (~6 pos/s): e.g. 2k ≈ 6 min, 10k ≈ 28 min

### D · Merge & coherence checks (optional merge; required uniformity)

- [ ] If merging: → `train.parquet` / `val.parquet` under `data/processed/labeled/`
- [ ] If **not** merging yet: still treat `lichess_labeled` + `lc0_labeled` as one policy (same schema + target)
- [ ] Single `teacher_network` across all rows / blocks
- [ ] `source` only in `{lichess, lc0}`; no ChessBench / Kaggle rows in production train set
- [ ] Split **by game**, not by row (no game leakage into val)
- [ ] Bucket counts logged (natural skew OK; flag if a bucket is empty)
- [ ] `manifest.json` complete per ASSETS §Format & quality rules (covers all blocks)

### E · Loader & smoke train (prove coherence)

- [ ] Dataset class accepts production schema (FEN→844 encode **or** optional precomputed features)
- [ ] One short train smoke on `train.parquet` (few epochs) — confirms columns + labels wire end-to-end
- [ ] Note: this is still not nnue-pytorch production; only pipeline validation

### F · Full Lichess volume (multi-session — not today)

- [ ] `download_lichess.py` (or documented manual pull) → `data/raw/lichess/` monthly PGN(.zst)
- [ ] Extract 10⁵–10⁶ FENs with documented `--sample-every` / filters
- [ ] Overnight / multi-day labeling job; resume-safe checkpoints
- [ ] Rebuild train/val + manifest at production scale

### G · Optional quality extras (after F)

- [ ] Precompute `white_features` / `black_features` (844) for train speed
- [ ] Teacher calibration check (sample val vs outcomes / Syzygy)
- [ ] Decide production net: stay on `fast` vs T1-256/BT4 (re-label if changed)

---

### Suggested “today / next session” slice (mini production set)

If time remains today, do in order:

1. Schema pass (A) on the two extract scripts + labeler  
2. Rebuild Lichess + Lc0 unlabeled tables (B) from data already on disk  
3. Label both with `791556` within a fixed budget (e.g. **all smoke FENs ~2.4k + Lc0 pilot ~1k** ≈ **10 min**) (C)  
4. Merge + manifest (D)  
5. Optional 1-epoch train smoke (E)  

That yields a **complete, uniform, coherent** dataset at **smoke/pilot scale** — not blueprint volume.

---

## Carry-over for next session

1. Production dataset checklist **A–E** (mini set) then **F** (full Lichess)
2. **nnue-pytorch** fork/adapt (844-dim bucketed) — or retrain pilot on teacher-labeled set
3. Qsearch depth limit / delta pruning per HCE (non-blocking)
4. TT / nodes-s benchmark (engine B remaining)
