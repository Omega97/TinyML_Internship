# 2026-06-26

## The Plan for Today

**Focus:** Weight sparsity experiment — prune a value net to 80% sparsity (20% non-zero weights) and export an int8 header for the Wio sketch.

1. **Sparse weight export script** — Add `scripts/prepare_wio_sparse.py` (magnitude pruning at SPARSITY=0.8) on top of the existing `prepare_wio_*` pipeline; emit `wio_int8_weights_sparse80.h`.
2. **Verify export** — Run the script on the default medium model; confirm actual sparsity ≈ 80% and header generates without errors.
3. **Device follow-up (optional)** — Flash sketch with sparse header; benchmark latency vs dense medium (expect similar until forward pass skips zero weights).

---

## Execution Log

- **Task 1 — sparse weight export script:** Added `apply_weight_sparsity()` to `scripts/wio_int8_common.py` (global magnitude pruning on weight tensors; biases untouched). Added `scripts/prepare_wio_sparse.py` — default `MediumValueMLP`, `--sparsity 0.8` (20% non-zero), outputs `wio_int8_weights_sparse80.h` + checkpoint/blob.
- **Task 2 — verify export:** `py -3.12 scripts/prepare_wio_sparse.py` on medium — **80.0%** sparsity achieved (21,313 / 106,560 weights non-zero). Header: `Arduino/Wio_TinyValueTest/wio_int8_weights_sparse80.h` (382 KB). Checkpoint: `models/checkpoints/tiny_value_wio_sparse80_int8.pt`.
- **Task 3 — trained small sparse model:** User ran `--model small --sparsity 0.8 --train --epochs 10` (51,329 params, 10,248 non-zero). Re-exported as **CSR header** (100 KB vs 176 KB dense zeros). Arduino: sparse CSR forward in `Int8ValueNet.cpp` (`SPARSE_WEIGHTS`), `config.h` → `wio_int8_weights_sparse80.h`.
- `py -3.12 scripts/prepare_wio_sparse.py --model huge --sparsity 0.8 --train --epochs 10`
