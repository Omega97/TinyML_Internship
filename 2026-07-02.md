# 2026-07-02

## The Plan for Today

Priorità: **chiudere le discrepanze** tra `src/tinymlinternship/features/` e [NOTES/SARDINE Engine Blueprint.md](NOTES/SARDINE%20Engine%20Blueprint.md) prima di aprire step 2.

1. **Audit features ↔ Blueprint** — mappare allineamento e gap (sezione sotto); aggiornare questa nota.
2. **Chiudere step 1 gate** (unico blocco funzionale rimasto sull'encoder):
   - [x] **Task 5** — golden FEN snapshots in `tests/test_features.py` (startpos, EP, EP mirror, bucket boundary, dual asimmetrico)
   - [x] **Task 6** — milestone: `pytest tests/test_features.py` (29 test), [NOTES/SARDINE commands.md](NOTES/SARDINE%20commands.md) aggiornato
3. **Doc drift minore nel codice** *(dopo Task 5–6)*:
   - [ ] `index_map.py` — riferimento obsoleto `NOTES/SARDINE 🐟.md` → Blueprint
   - [ ] `encoder.py` — docstring castling: diritti letti da `base` + swap K/Q se mirror, non da `view` diretto
4. **Step 2 — search skeleton su PC** *(prossima sessione)* — `engine/`, perft, eval hook stub, formato TT.
5. **Dati Lc0** *(parallelo)* — `download_lc0.py`, subset **~1–2 GB**.

---

## Discrepanze `features/` ↔ Blueprint

Riferimento blueprint: sezioni **Input features**, **Output buckets**, **MoE + NNUE** (solo input lato encoder).

### Allineato (nessuna azione)

| Blueprint | Codice |
|-----------|--------|
| 716 = 768 − 32 pawn + − 32 king-self + 4 castling + 8 EP | `FEATURE_DIM`, `piece_square_count()==704`, `meta_base()==704` |
| Pawn rank 1/8 assenti dall'index map | `is_pawn_rank_inactive`, `piece_square_index` → `None` |
| King prospettiva 64→32; king nemico 64 pieno | `king_self_index` / `king_enemy_index`, test alias g1≡b1 vs g8≠b8 |
| Mirror orizzontale se re prospettiva su file e–h | `mirror.py` → `flip_horizontal` |
| Dual-perspective: White POV + `board.mirror()` per Black | `encode_dual()` |
| 8 bucket queen-split (tabella p + queen) | `bucket.py` — confini 12 / 13–21 / 22–27 / 28–31 / 32 |
| Moduli `index_map`, `encoder`, `mirror`, `bucket` | `src/tinymlinternship/features/` |
| Test encoder su PC | **29 passed** (`pytest tests/test_features.py`) |

### Gap aperti (priorità)

| # | Blueprint dice | Stato `features/` | Azione |
|---|----------------|-------------------|--------|
| ~~**G1**~~ | Gate step 1: test robusti su `tests/test_features.py` | Golden FEN con snapshot indici completi — **chiuso** | — |
| ~~**G2**~~ | Doc milestone in commands | `SARDINE commands.md` → **29 test** | — |
| **G3** | Pipeline step 1: encoder **PC + device parity** | Solo Python; nessun port C/C++ index map | Step 2+ / port Wio |
| **G4** | Incremental add/sub; refresh al crossing file e–h | `mirror.py` documenta il vincolo; **nessuna API** `feature_diff(move)` | Step 5 (post-search) |
| **G5** | Bridge verso nnue-pytorch / export int8 | `encode_dual` → `list[int]` sparse; **nessun** dense vector, LUT tanh, né dataloader | Step 4 training |
| ~~**G6**~~ | Blueprint repo status: encoder «in corso» | Step 1 gate chiuso — aggiornare status nel Blueprint | Doc sync |

### Fuori scope `features/` (blueprint, altri moduli)

- NNUE 716→16→1, CReLU hidden, **tanh LUT** output, int8 — non in `features/`
- Search, TT, Lc0 download — step 2–4

---

## Stato repo (sintesi)

| Fatto | Mancante |
|-------|----------|
| Encoder 716 + bucket + golden FEN gate (G1–G2 chiusi) | Device parity C, incremental API |
| Dual-perspective sparse, mirror/EP/castling fixati | Bridge nnue-pytorch, tanh LUT export |
| 29 test feature encoder | Search, TT, `download_lc0.py` |
| Archive pre-SARDINE in `legacy/pre-sardine/` | Search, TT, `download_lc0.py` |

**Step 1 chiuso** — gate test/doc ok; prossimo: search skeleton (step 2).

---

## Execution Log

- **Bug fixes (ai-feed review).** King self/enemy planes, EP/castling coordinate frame, 17 test passing. Vedi [2026-07-01.md](2026-07-01.md).
- **Obiettivi rivisti.** Elo gate **≥ 1700**. Piano dati: subset Lc0 **1–2 GB** + label Stockfish — in Full locked spec: [[SARDINE Engine Blueprint]].
- **Blueprint NNUE output.** Attivazione finale **tanh via LUT** (hidden resta CReLU) — aggiornato nel Blueprint.
- **Audit features ↔ Blueprint.** Mappatura allineamento/gap (sezione sopra); piano riprioritizzato su G1–G2.
- **G1–G2 chiusi.** Golden FEN snapshots (`test_golden_encode_perspective`, `test_golden_encode_dual_asymmetric`, `test_golden_bucket_id`); 29 test passing; `SARDINE commands.md` aggiornato.