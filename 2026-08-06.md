# 2026-08-06

## Context

**Progress map:** [PROJECT.md](PROJECT.md) (A1).  
**Ieri:** [DAILY-NOTES/2026-08/2026-08-05.md](DAILY-NOTES/2026-08/2026-08-05.md) — F3 `SingleHeadNNUE` path + mini train (`single_W128_mini_ep30`, best val_mse **0.196**) + d1 ACPL gate **fail** (~583 / Elo floor 400). Pilot multi-head resta reference student. HCE d2 10-game gate ticked (Elo **~2646**). Timed search (`search_timed` / `--movetime`) aggiunto in sessione.

**Database mini (path):** `data/processed/labeled/{train,val}.parquet` + `manifest.json` (Lc0 + Lichess smoke, teacher `791556`).

Carry-over: **data scale** prima di un retrain single-head credibile. Niente Wio / thesis / nnue-pytorch fork oggi salvo che il path lo richieda.

## Plan for today

1. **Data scale kickoff** — path per full Lichess monthly dump (`data/raw/lichess/`) e/o design dedup+multiplicity; documentare comandi e volume target (PROJECT §Dataset ancora aperti).

2. **Se dump/path pronto:** FEN extract smoke + (opzionale) label subset Lc0 → blocchi labeled.

3. **Altrimenti / in parallelo:** retrain single-head con early-stop/reg sul mini (solo se utile; volume resta il vincolo) **oppure** documentare baseline train recipe per quando i dati scalano.

4. **ACPL re-gate** solo se esce un nuovo ckpt più forte del mini fail (altrimenti skip).

5. **Sync trackers** — PROJECT/TODOs/ASSETS + log qui; carry-over a fine sessione.

## Execution log

_(vuoto — append man mano)_

## Carry-over

_(compilare a fine sessione)_

## Parking lot

| Item                                     | Note                                     |
| ---------------------------------------- | ---------------------------------------- |
| Full Lichess dump + re-label             | PROJECT §Dataset · priorità oggi         |
| Dedup + multiplicity                     | PROJECT §Dataset [ ]                     |
| Single-head strength ≥ pilot (~139 ACPL) | dopo volume                              |
| Keep `pilot_W128_844` default demos      | finché single-head non batte ladder      |
| L1 prune / PTQ / nnue-pytorch            | dopo baseline giocabile                  |
| §D bucket ablation / thesis (J1)         | dopo Elo path                            |
| Wio / device                             | hardware                                 |
| Search v1 remainder (TT, LMR, …)         | TODOs §7c · `search_timed` già in v0.3.1 |
| Custom SARDINE weights in Cfish          | later / optional                         |
