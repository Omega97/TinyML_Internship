# 2026-07-01

## The Plan for Today

1. ~~Reorganize repo: archive pre-SARDINE code out of the active tree.~~
2. SARDINE step 1 — feature encoder (Python first):
   - [x] **Task 1** — pin 716-dim index map (`index_map.py`)
   - [x] **Task 2** — horizontal king mirroring helper (`mirror.py`)
   - [x] **Task 3** — `encode_perspective()` / `encode_dual()` (`encoder.py`)
   - [x] **Task 4** — `bucket_id()` (`bucket.py`)
   - [ ] Task 5 — full tests + golden FENs
   - [ ] Task 6 — package wiring
   - [ ] *(follow-up)* C++ encoder + parity harness

## Execution Log

- **Repo cleanup.** Moved the old TinyML export pipeline (768-feature value MLPs, policy net, Wio int8 benchmark sketch, export scripts, checkpoints) to `legacy/pre-sardine/`. Active tree is now `src/tinymlinternship/` (config + empty `models/` for new code), `scripts/download_data.py`, `scripts/plot_piece_count_distribution.py`, and `tests/test_data.py`. Updated `settings.py` and `.gitignore` accordingly.
- **Docs.** Added a brief SARDINE pipeline summary to [README.md](README.md) (targets, architecture, build order, active vs legacy layout).
- **Step 1 / task 1.** Added [src/tinymlinternship/features/index_map.py](src/tinymlinternship/features/index_map.py): 704 piece-square indices (128 pawn slots with 32 inactive ranks, 512 minor/major, 64 mirrored king slots) + 4 castling + 8 EP = **716**. Pawn ranks 1/8 return `None`; king squares alias by `min(file, 7-file)`.
- **Step 1 / task 2.** Added [src/tinymlinternship/features/mirror.py](src/tinymlinternship/features/mirror.py): `perspective_board()` / `mirror_for_perspective()` flip horizontally when the perspective king is on files e–h. Smoke tests in [tests/test_features.py](tests/test_features.py).
- **Step 1 / task 3.** Added [src/tinymlinternship/features/encoder.py](src/tinymlinternship/features/encoder.py): sparse `encode_perspective()` (pieces on king-mirrored view; castling/EP from original board) and `encode_dual()` (White POV / Black POV via `board.mirror()`). Exported from `features/__init__.py`. Startpos → 36 active features; encoder tests in [tests/test_features.py](tests/test_features.py).
- **Step 1 / task 4.** Added [src/tinymlinternship/features/bucket.py](src/tinymlinternship/features/bucket.py): `bucket_id()` from piece count + queen presence (8 queen-split buckets); helpers `piece_count()`, `has_queen()`. Boundary tests in [tests/test_features.py](tests/test_features.py) (13 tests total).

## SARDINE pipeline (reference)

Wio Terminal chess engine — **≥ 2000 Elo**, **~1 s/move**, **192 KB RAM**.

1. Feature encoder — pruned **716** features (pawn-rank zeros, king mirroring, castling, en passant)
2. Search skeleton on PC (C++) — perft, eval hooks, TT format benchmark
3. Train bucketed NNUE — `716 → 16 → 1`, 8 queen-split buckets; Lc0 data; nnue-pytorch → int8
4. Queen-split ablation — per-bucket MSE vs piece-count baseline
5. Incremental accumulators on device
6. Port search + NNUE to C — benchmark `-O3` vs `-Os`
7. Full search stack — quiescence, futility, LMR, null-move, lazy eval, ID, SPSA
8. **Elo gate** — iterate or proceed to v2 (policy head, UCI polish)

Full locked spec: [[SARDINE Engine Blueprint]]
