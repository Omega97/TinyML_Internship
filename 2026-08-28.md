# 2026-08-28

## Context

DGX Spark is on the lab LAN (`140.105.28.87`, aarch64, 128 GB unified, 4 TB NVMe). Goal §2 is in progress on the laptop (`--fast` DualHidden, best `test_ce=0.696645`). Carry-over from [DAILY-NOTES/2026-08/2026-08-25.md](DAILY-NOTES/2026-08/2026-08-25.md). Session is: what to run on Spark, whether a new GPU trainer is needed, how to copy data.

## Plan for today

1. Decide Spark vs laptop split: Spark = Lc0 labeling + dump conversion + full (non-`--fast`) DualHidden / FFNN trains; laptop = Cfish / Wio / code edit.
2. Bootstrap Spark: git clone code, Linux ARM Lc0 + CUDA PyTorch, do **not** copy `lc0.exe`.
3. Copy only training slices (`data/processed/board_eval/fen_value_visits/`, ~3 GB) and either scp or re-download the 27 GB Lichess dump.
4. Smoke `train_nnue.py` on CUDA (existing script already picks GPU). Then full DualHidden without `--fast`.
5. After smoke: stream remaining dump games + optional depth-2 relabel on Spark.

## Execution log

- Read `PRIVATE/Nvidia Spark Notes.md`, Goal §2 status, `scripts/train_nnue.py`. Spark is GB10 Grace Blackwell (20-core Arm + Blackwell, 128 GB unified). Trainer already uses `cuda` if available. `LC0_BINARY` is hardcoded to `lc0.exe` (Windows). Processed slices ~3.17 GB; raw ~29.6 GB (dump 27.06 GB).
- **npz bundle.** `scripts/bundle_slice_npz.py` copies each slice `features.npz` to `data/processed/board_eval/fen_value_visits/npz_bundle/<slice>/features.npz` and writes `npz_bundle.zip` (ZIP_STORED). `--no-copy` zip-only. Tests: `tests/test_bundle_slice_npz.py` (3 passed).

## Carry-over

1. Rebuild npz puzzle depth-2 (laptop or Spark after copy).
2. DualHidden non-`--fast` L1 128 / L2 256, `--lr-end 0.001`, new run-name — **run this on Spark**.
3. Align parquet vs npz on val `…_0-5000_d90`.
4. FFNN baseline vs DualHidden.
5. PESTO tables (not Spark-critical).
6. Make `LC0_BINARY` OS-aware (`lc0` on Linux).

## Parking lot

- Goal §3 skill vectors / clustering once a full vanilla net is trained on Spark.
- Do not ship Cfish eval development to Spark (search target is tiny hardware).
