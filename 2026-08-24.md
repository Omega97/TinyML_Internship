# 2026-08-24

## Context

Continue Goal §2 after the 2026-08-23 slice-folder train (test MSE still worse than dummy-0). Carry-over from [DAILY-NOTES/2026-08/2026-08-23.md](DAILY-NOTES/2026-08/2026-08-23.md).

## Plan for today

1. Count unique EPDs vs total rows in `data/processed/board_eval/fen_value_visits/` (join is stale after `30000-32000`).
2. Goal §2: lr / weight-decay / early-stop on `best.pt`; optional visit-weighting.
3. Stream-convert more of `lichess_db_standard_rated_2026-07.pgn.zst` (do not inflate the PGN).

## Execution log

- **Count script.** `scripts/count_fen_value_visits.py` walks slice JSON/parquet (same discovery as join; parquet wins; EPD = FEN fields 1–4). Does not write the joined table. Test: `test_count_unique_and_total_rows`.
- **Live totals (22 slices):** **2 304 804** total rows, **2 215 519** unique EPDs, visits sum **2 568 373**, cross-slice overlap **89 285** rows. Includes `30000-32000` (121 423 rows). Command: `py -3.12 -u scripts/count_fen_value_visits.py`.

## Carry-over

1. Goal §2: test MSE rises after epoch 1. Next: lr / weight-decay / early-stop (use `best.pt`), optional visit-weighting, label noise (`nodes 1`).
2. Stream-convert more of `lichess_db_standard_rated_2026-07.pgn.zst` (do not inflate the PGN).
3. Remaining Goal §1: leftover Kaggle JSON, puzzles 16k labels. Unique-EPD count is **2 215 519** (count script; join/manifest not re-run).

## Commands run

```powershell
py -3.12 -m pytest tests/test_fen_value_visits.py -q
py -3.12 -u scripts/count_fen_value_visits.py
```
