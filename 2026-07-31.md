# 2026-07-31

## Context

Integrato **Cfish** nel repo: sorgenti + binario + rete NNUE in [`src/cfish/`](../../src/cfish/), launcher [`run-cfish.bat`](../../run-cfish.bat) (cwd = `src/cfish` così `EvalFile` risolve `nn-62ef826d1a6d.nnue`).  
Obiettivo della sessione: validare Cfish come baseline forte (smoke → self-play → ACPL con Stockfish), poi ripetere lo stesso protocollo su una delle nostre NNUE SARDINE.

## Plan for today

1. **Smoke test Cfish** — avvio UCI (`uci` / `isready` / `go`), verifica NNUE Hybrid, eventuale fix path/`run-cfish.bat`
2. **Self-play Cfish** — generare partite Cfish vs Cfish (PGN/GIF o log), salvare artefatti sotto `images/games/` o path dedicato
3. **Centipawn loss con Stockfish** — valutare le partite self-play con ACPL Stockfish (`eval_bot_acpl.py` / `eval_game_elo.py`, Stockfish su PATH)

**Dopo (prossima sessione / follow-up):** stesso pipeline su una NNUE nostra (checkpoint SARDINE) — self-play + ACPL Stockfish per confronto vs Cfish.

## Execution log

- Integrato Cfish in `src/cfish/` (+ `run-cfish.bat` al root). Smoke UCI preliminare: `uciok` / `readyok` da quella directory.

## Carry-over

1. Ripetere smoke → self-play → ACPL Stockfish con una NNUE SARDINE (`--eval nnue`)
2. Confronto Cfish vs NNUE (ACPL / Elo euristico)
