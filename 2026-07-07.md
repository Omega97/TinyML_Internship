# 2026-07-07

## Context (snapshot)

| Area            | Stato                                                                               |
| --------------- | ----------------------------------------------------------------------------------- |
| **Encoder**     | ✅ **844** dim (716 base + 64 under-attack + 64 king-attacker), dual POV, 8 bucket   |
| **Engine**      | ✅ v0.3 — perft d5, alpha-beta, capture quiescence (`engine/search.py`)              |
| **Teacher Lc0** | ✅ `fast` (791556) default · depth-1 ~1 s/ply · depth-2 ~1 min/ply (molto più forte) |
| **Teacher HF**  | ✅ chess_lite + artoria-small — ~2 ms/eval, gioco debole a depth-1                   |
| **Lc0 data**    | ✅ ~1.15 GiB raw + pilot `positions.parquet` (no labels)                             |
| **ChessBench**  | ⚠️ pilot smoke only — pipeline ufficiale = Lichess FEN + label Lc0 on-the-fly          |
| **Blueprint**   | ✅ training pipeline aggiornata (nnue-pytorch, natural buckets, gradual prune, PTQ)  |
| **NNUE**        | ✅ `pilot_W128_844` val_mse **0.056** — collegato all'engine (`--eval nnue`)         |
| **Bot eval**    | ✅ gate depth-1 ACPL — **121.1 cp** → Elo **1644** (1112–2175), 80 mosse             |
| **Stockfish**   | ✅ SF 18 avx2 in `models/teacher/stockfish/stockfish.exe`                            |
| **Test suite**  | ✅ **92 test** — encoder + NNUE + `bot_eval` ACPL                                    |

Blueprint: [NOTES/SARDINE Engine Blueprint.md](NOTES/SARDINE%20Engine%20Blueprint.md) · [NOTES/Models.md](NOTES/Models.md) · [TODOs.md](TODOs.md)

---

## The Plan for Today

Obiettivo: avviare **step C** (training NNUE pilot su ChessBench) e portare avanti search carry-over.

1. **NNUE training pilot — ChessBench**
   - [x] Verificare `data/processed/chessbench/splits/train.parquet` — 59 688 × 8 colonne OK
   - [x] Setup training PyTorch — `src/tinymlinternship/nnue/` + `scripts/train_nnue.py`
   - [x] Primo run 5 ep — val_mse **0.058**, val_mae **0.162** (94k params, W=128)

2. **Training pipeline (blueprint §Training pipeline) — decisioni**
   - [x] **Data:** Lichess PGN → FEN; label on-the-fly Lc0 latest net (UCI WDL → `W−L`)
   - [x] **Framework:** nnue-pytorch adattato a 844-dim bucketed (sostituisce pilot PyTorch)
   - [x] **Bucketing:** distribuzione naturale Lichess — no resampling stratificato
   - [x] **L1:** dense W∈{128,256} + **gradual prune** in training (70–80%)
   - [x] **Quant:** PTQ first; QAT solo se MSE>0.01 o Elo drop >30
   - [x] **Train:** 100 ep fissi; best + final checkpoint
   - [x] Allineare repo a §Training pipeline (blueprint, README, TODOs, Datasets, Models, Lc0 pipeline, Commands)

3. **Gate depth-1 — ACPL (§Bot Evaluation)**
   - [x] `scripts/eval_bot_acpl.py` + `src/tinymlinternship/bot_eval/` (Stockfish analizza self-play)
   - [x] Stockfish 18 installato (`models/teacher/stockfish/`)
   - [x] Primo run NNUE depth-1, qsearch off — vedi risultati sotto
   - [ ] Baseline HCE stesso protocollo (confronto side-by-side)
   - [ ] Sunfish come riferimento calibrazione (B1 blueprint)

4. **Search skeleton — carry-over**
   - [ ] `record_engine_game.py` — flag `--depth`
   - [ ] GIF SARDINE depth ≥ 2 · TT prototype

5. **Encoder tattico 844-dim**
   - [x] Piani under-attack + king-attacker (`tactical.py`, `index_map.py`)
   - [x] Retrain `pilot_W128_844` + doc sync (README, blueprint, TODOs)

6. **Gate rapido**
   - [x] `py -3.12 -m pytest tests/ -q` — **92 passed** (incl. `test_bot_eval_acpl.py`)

---

## Execution Log

- **train.parquet verify** — 59 688 righe, 8 colonne (`fen`, `bucket_id`, `white/black_features` sparse 716, `expected_reward` ∈ [-1,+1]); manifest coerente.
- **NNUE module** — `BucketedNNUE`: shared L1 716→W, dual POV concat 2W, 8 expert heads, CReLU + tanh; `ChessbenchDataset` da parquet.
- **Pilot training** — `py -3.12 scripts/train_nnue.py --epochs 5 --run-name pilot_W128_chessbench`: ep5 val_mse=0.058, val_mae=0.162; checkpoint `models/checkpoints/nnue/pilot_W128_chessbench/best.pt`.
- **Tests** — `test_nnue_model.py` (4 test) verdi.
- **README refactor** — `Project Report.md` creato (§Report del Progetto spostato); README snellito, §SARDINE Pipeline allineata al blueprint (v0.3, W=128/256, ChessBench, NNUE pilot).
- **NNUE → engine** — `eval_nnue.py` + `make_eval_fn`; `--eval nnue` su `run_engine.py` / `record_engine_game.py`; smoke: `bestmove d2d4 score cp 23`.
- **NNUE self-play depth-2** — `record_engine_game.py --eval nnue --depth 2`: 80 ply in ~95 s → `images/sardine_game.gif` (245 KB) + PGN.
- **NNUE architecture plot** — `scripts/plot_nnue_architecture.py` → `plots/sardine_nnue_architecture.png`.
- **Encoder 844** — tactical planes: under-attack + king-attacker (64+64); `tactical.py`; retrain `pilot_W128_844` val_mse=0.056 (110k params).
- **Doc sync** — README, blueprint, TODOs, Models, Datasets, Lc0 pipeline, SARDINE commands: 716 → 844.
- **AI-SKILLS** — `AI-SKILLS/sardine-repo/SKILL.md` (repo + agent cards + Commands); schede `NOTES/agents/` (hce-d2, nnue-d1/d2).
- **Blueprint — training pipeline** — confermate 9 stage (tabella §Training pipeline): Lc0 on-the-fly, nnue-pytorch, natural buckets, gradual prune, PTQ→QAT fallback, 100 ep, depth-1 baseline, SPSA solo search, export int8+LUT. Pilot ChessBench/PyTorch resta smoke, non path produzione.
- **Doc sync §Training pipeline** — allineati blueprint (mermaid, L1, buckets, teacher, quant), README, TODOs, Goal, Datasets, Models, Lc0 pipeline, Commands; docstring smoke su `train_nnue.py` / `prepare_lc0_dataset.py`.
- **Gate depth-1 ACPL** — metodo §Bot Evaluation (A1): self-play → Stockfish giudica le mosse, non winrate vs avversario. Implementati `bot_eval/acpl.py`, `scripts/eval_bot_acpl.py`, `tests/test_bot_eval_acpl.py`.
- **Stockfish install** — SF 18 avx2 da GitHub release `sf_18` → `models/teacher/stockfish/stockfish.exe`.
- **Primo run gate NNUE** — `eval_bot_acpl.py --eval nnue --depth 1 --max-plies 80 --no-quiescence --sf-movetime-ms 100` (~21 s):
  - **ACPL 121.1 cp** (σ=242.5) su 80 mosse
  - **Elo euristico 1644** (range **1112–2175**, formula `2855 − 10×ACPL`)
  - Peggiori CPL: ply 40 `g1g2` (1048), ply 33 `f4g5` (1021), ply 37 `e6f5` (982)
  - Artefatti: `plots/nnue_d1_gate.pgn`, `plots/nnue_d1_gate_acpl.json`
  - Nota: pilot ChessBench/SF16 — pavimento indicativo, non stima produzione Lc0
