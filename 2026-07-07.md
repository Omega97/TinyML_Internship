# 2026-07-07

## Context (snapshot)

| Area            | Stato                                                                               |
| --------------- | ----------------------------------------------------------------------------------- |
| **Encoder**     | ✅ 716 dim, dual POV, 8 bucket — `src/tinymlinternship/features/`                    |
| **Engine**      | ✅ v0.3 — perft d5, alpha-beta, capture quiescence (`engine/search.py`)              |
| **Teacher Lc0** | ✅ `fast` (791556) default · depth-1 ~1 s/ply · depth-2 ~1 min/ply (molto più forte) |
| **Teacher HF**  | ✅ chess_lite + artoria-small — ~2 ms/eval, gioco debole a depth-1                   |
| **Lc0 data**    | ✅ ~1.15 GiB raw + pilot `positions.parquet` (no labels)                             |
| **ChessBench**  | ✅ **62 829 pos** etichettate + splits train/val in `data/processed/chessbench/`     |
| **Blueprint**   | ✅ expected reward, L1 W=128/256 + magnitude prune                                   |
| **NNUE**        | ✅ pilot W=128 — val_mse **0.058** / mae **0.162** (`models/checkpoints/nnue/pilot_W128_chessbench/`) |
| **Test suite**  | ✅ **77 test** — +4 NNUE (`test_nnue_model.py`)                                      |

Blueprint: [NOTES/SARDINE Engine Blueprint.md](NOTES/SARDINE%20Engine%20Blueprint.md) · [NOTES/Models.md](NOTES/Models.md) · [TODOs.md](TODOs.md)

---

## The Plan for Today

Obiettivo: avviare **step C** (training NNUE pilot su ChessBench) e portare avanti search carry-over.

1. **NNUE training pilot — ChessBench**
   - [x] Verificare `data/processed/chessbench/splits/train.parquet` — 59 688 × 8 colonne OK
   - [x] Setup training PyTorch — `src/tinymlinternship/nnue/` + `scripts/train_nnue.py`
   - [x] Primo run 5 ep — val_mse **0.058**, val_mae **0.162** (94k params, W=128)

2. **Dataset strategy (post-pilot)**
   - [ ] Valutare ChessBench train full (1.1 TB) vs mix Lc0 + ChessBench
   - [ ] Decidere se serve labeling Lc0 o solo dump pre-etichettati

3. **Search skeleton — carry-over**
   - [ ] `record_engine_game.py` — flag `--depth`
   - [ ] GIF SARDINE depth ≥ 2 · TT prototype

4. **Gate rapido**
   - [ ] `py -3.12 -m pytest tests/ -q`

---

## Execution Log

- **train.parquet verify** — 59 688 righe, 8 colonne (`fen`, `bucket_id`, `white/black_features` sparse 716, `expected_reward` ∈ [-1,+1]); manifest coerente.
- **NNUE module** — `BucketedNNUE`: shared L1 716→W, dual POV concat 2W, 8 expert heads, CReLU + tanh; `ChessbenchDataset` da parquet.
- **Pilot training** — `py -3.12 scripts/train_nnue.py --epochs 5 --run-name pilot_W128_chessbench`: ep5 val_mse=0.058, val_mae=0.162; checkpoint `models/checkpoints/nnue/pilot_W128_chessbench/best.pt`.
- **Tests** — `test_nnue_model.py` (4 test) verdi.
- **README refactor** — `Project Report.md` creato (§Report del Progetto spostato); README snellito, §SARDINE Pipeline allineata al blueprint (v0.3, W=128/256, ChessBench, NNUE pilot).
