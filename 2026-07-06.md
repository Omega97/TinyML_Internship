# 2026-07-06

## Context (snapshot)

| Area | Stato |
|------|-------|
| **Encoder** | ✅ 716 dim, dual POV, 8 bucket — `src/tinymlinternship/features/` |
| **Engine** | ✅ v0.3 — perft d5, alpha-beta, capture quiescence (`engine/search.py`) |
| **Teacher Lc0** | ✅ `fast` (791556) default · depth-1 ~1 s/ply · depth-2 ~1 min/ply (molto più forte) |
| **Teacher HF** | ✅ chess_lite + artoria-small — ~2 ms/eval, gioco debole a depth-1 |
| **Lc0 data** | ✅ ~1.15 GiB raw + pilot `positions.parquet` (no labels) |
| **ChessBench** | ✅ test split scaricato → **62 829 pos etichettate** in `data/processed/chessbench/` |
| **Blueprint** | ✅ expected reward, L1 W=128/256 + magnitude prune |
| **NNUE** | ⚠️ dataset pilot pronto — training non ancora avviato |
| **Test suite** | ✅ **73 test** — ultimo run oggi, tutti verdi (~71 s) |

Blueprint: [NOTES/SARDINE Engine Blueprint.md](NOTES/SARDINE%20Engine%20Blueprint.md) · [NOTES/Models.md](NOTES/Models.md) · [TODOs.md](TODOs.md)

---

## The Plan for Today

Obiettivo: allineare codice/doc al blueprint e sbloccare **step C** (dataset + teacher labels).

1. **Allineamento doc dopo aggiornamenti blueprint**
   - [x] [TODOs.md](TODOs.md), `index_map.py` / `encoder.py`, [ai-feed.md](ai-feed.md)

2. **Dataset survey — teacher expected reward**
   - [x] Survey [NOTES/Datasets.md](NOTES/Datasets.md) · teacher Lc0 in [NOTES/Models.md](NOTES/Models.md)

3. **Pipeline label su pilot Lc0**
   - [ ] ~~`scripts/label_positions.py`~~ — **deprioritizzato** (labeling UCI troppo lento)
   - [x] Percorso alternativo: **ChessBench** pre-etichettato (SF 16 `win_prob`)

4. **Search skeleton — carry-over**
   - [ ] `record_engine_game.py` — flag `--depth`
   - [ ] GIF SARDINE depth ≥ 2 · TT prototype

5. **Gate rapido**
   - [x] `py -3.12 -m pytest tests/ -q` — **73 passed**

---

## Execution Log

- **Blueprint — dataset & teacher** — [NOTES/SARDINE Engine Blueprint.md](NOTES/SARDINE%20Engine%20Blueprint.md): expected reward, Lichess+Lc0, L1 W=128/256 + magnitude prune.
- **Punto 1–2 — doc + survey** — [TODOs.md](TODOs.md), [NOTES/Datasets.md](NOTES/Datasets.md), [NOTES/Models.md](NOTES/Models.md).
- **Teacher Lc0 install** — `scripts/download_teacher.py` (lc0 v0.32.1 + BT4); default rete **`fast` (791556)**; `eval_lc0.py`, `record_teacher_game.py` (`--network`, `--depth`, live moves, auto GIF path).
- **Depth-2 playtest** — depth-2 nettamente migliore ma ~1 min/ply; labeling batch via UCI scartato → priorità dump pre-etichettati.
- **HF teacher bench** — scaricati chess_lite + artoria-small (`download_hf_teacher.py`); chess_lite ~1.8 ms/eval (~3.5× artoria); `eval_chess_lite.py`, `record_hf_game.py`; GIF `images/games/hf_chess_lite_depth1_*.gif`.
- **HF playtest 400 ply** — chess_lite velocissimo ma gioco **orribile** a depth-1; utile solo smoke latency, non demo qualità.
- **ChessBench** — scaricato test split (`download_chessbench.py`): `state_value_data.bag` (62 829 pos) + `action_value_data.bag` (141 MB). Analisi: `win_prob` ∈ [0,1] = win% SF16 (non cp, non WDL); `study_chessbench.py`.
- **ChessBench → NNUE training** — `prepare_chessbench_dataset.py` → `data/processed/chessbench/positions.parquet` + splits train/val; colonne `white_features`, `black_features` (sparse 716), `expected_reward` = `2·win_prob−1` (STM); `chessbench_preprocess.py`; test `test_chessbench_preprocess.py`.
- **Models.md** — aggiornato con depth-2, HF debole, ChessBench label semantics.
- **Test suite** — 73 test (`test_eval_lc0.py`, `test_chessbench_preprocess.py`); full run verde.

---

## Carry-over (domani / prossima sessione)

1. **Step C — training NNUE** su `data/processed/chessbench/splits/train.parquet` (pilot 60k pos).
2. Valutare ChessBench train full (1.1 TB) o mix Lc0 + ChessBench dopo conversione label.
3. Search carry-over: `record_engine_game.py --depth`, TT, GIF SARDINE depth-2.