# 2026-06-24

## The Plan for Today

**Focus:** Push real inference throughput on the Wio Terminal and learn **hardware–software synergy** — how memory hierarchy, data layout, and compute style (int vs float) interact on a Cortex-M4F MCU.

**Learning lens:** For each optimization, answer: *what hardware resource does this relieve (flash bus, FPU, RAM, loop control)?* Log the answer in the execution log.

1. **Establish an honest baseline** — Flash the current sketch (`Wio_TinyValueTest`, active model: small) and record steady-state evals/s, free RAM, and flash usage. Run the same model with Serial/TFT updates disabled to isolate inference-only throughput vs display overhead.
2. **Measure sparse-L1 impact** — Re-benchmark after the existing sparse L1 guard (`x[j] > 0.5f`). Compare small vs medium on device; confirm throughput now scales with model size (fewer active features → fewer `pgm_read_byte` stalls).
3. **Profile the bottleneck** — On PC, run `scripts/benchmark_rate.py` on the small checkpoint; on device, time a single `evaluate()` call with a minimal loop (no display). Document whether flash-fetch latency or loop/FPU work dominates.
4. **Integer path experiment (LUT vs float)** — Prototype an int8 accumulate + lookup-table dequant path for L1 (skip per-weight float conversion). Compare evals/s and one FEN parity check against the current float path.
5. **Sparse L2/L3 + float activations** — Drop `(h > 0.5f ? 1 : 0)` binarization in hidden layers; add sparse guards where activations are zero. Re-benchmark and note accuracy/parity delta.
6. **Memory-hierarchy tweak** — Copy the hottest weight slice (e.g. L1 row or full L1 block) from PROGMEM into RAM at boot; measure evals/s gain vs flash-only baseline.
7. **Update performance table** — Append today's before/after numbers to `NOTES/Performance.md`; add a short "hw–sw synergy" note on which resource each change actually freed.
8. **Summarize the synergy lesson** — Write 3–5 bullet takeaways: when software choices matter on MCU (sparse input, int math, RAM staging) vs when they do not (wider layers with same flash bandwidth).

---

## Execution Log

- **Task 1 — optimized forward pass benchmark:** Re-flashed improved `Wio_TinyValueTest` sketch. Per-call latency: **53 ms → 45 ms** (~15% faster). Smallest model (nano): **1.8 ms → 1.4 ms** (~22% faster). Throughput gain confirms software changes are relieving real inference cost, not just measurement noise.
- **Display unit clarification:** Screen shows `706.44K` but `Benchmark.cpp` computes raw evals/s (`1000 * deltaEvals / dtMs`). The trailing `K` in `WioBoard.cpp` is a label only — **706.44K ≈ 706 evals/s**, not 706,440. Per-call time ≈ **1.4 ms** (1/706), not 1.4 μs.
- **Display fix:** Removed misleading `K` suffix from `WioBoard::showEvalsPerSec` — now prints raw avg evals/s on Serial and TFT.
- **Task 1 — nano baseline (confirmed):** **707.5 evals/s** (~1.41 ms/call). Display fix validated; rate matches prior 1.4 ms estimate.
- **Task 2 — full model sweep (latency ms):** nano **1.4**, tiny **2.8**, small **5.8**, medium **11.4**, big **22.6**, huge **45.0**. ~2× latency per tier — throughput now scales with model size; flash-fetch count dominates. Updated `NOTES/Performance.md`.